#include "zega.h"

void ShaderSystem::apply_glow(float x, float y, float intensity) {
    // Logic for calculating radial bloom intensity at impact coordinates
    // Used to feed alpha-blending values back to the Python layer
}

void ShaderSystem::apply_chromatic_aberration(float strength) {
    // Logic for offsetting RGB channels based on star gravity intensity
}